{
  "name": "hakai-structure-finder",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Offline-first fishing structure finder for Hakai Passage (BC Central Coast)",
  "scripts": {
    "sync-data": "bash sync-data.sh",
    "dev": "npm run sync-data && vite",
    "build": "npm run sync-data && vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11",
    "vite-plugin-pwa": "^0.21.1"
  }
}
