# prototype/

This directory should hold **`hakai-structure-finder.jsx`** — the working prototype of
the terrain-analysis + scoring + chart-rendering engine (synthetic grid, React canvas)
referenced throughout `CLAUDE.md`.

**It is not in the repo yet.** It didn't come through when the project was seeded (only
the CLAUDE.md spec and the planning screenshots did). Drop the `.jsx` here before
starting **M2** — the app phases *port* its renderer, terrain analysis, and scoring
weights rather than rewriting them from scratch (per CLAUDE.md).

M1 (the `pipeline/` bathymetry work + throwaway viewer) does **not** depend on this
file, so it isn't blocking right now.
